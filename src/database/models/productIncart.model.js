export class ProductInCart {
    constructor({ _id, name, price, stock, url }) {
        this._id = _id,
            this.name,
            this.price,
            this.stock,
            this.url,
            this.quantity
    }
}